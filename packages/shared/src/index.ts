// Root entry — re-exports config loader + the chain and api barrels.
// Consumers may also import the subpaths directly:
//   @onchain-novel/shared/chain
//   @onchain-novel/shared/api
export * from "./config.js";
